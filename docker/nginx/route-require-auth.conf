# This nginx conf file is included in any route that we want to ensure auth is present.
#
# Ref: https://oauth2-proxy.github.io/oauth2-proxy/docs/configuration/overview#configuring-for-use-with-the-nginx-auth_request-directive

auth_request /oauth2/auth;
# Use named location @error401 (defined at server level in nginx.conf) so we
# can forward the original URI to oauth2-proxy in the `X-Auth-Request-Redirect`
# header. oauth2-proxy encodes that into the LaunchPad `state` parameter,
# ensuring the browser is redirected back to the correct URL after auth.
#
# - A plain `error_page 401 = /oauth2/sign_in;` loses $request_uri at redirect
#   time, causing oauth2-proxy to return to its own host instead of CODA's.
# - A `return 302 /oauth2/sign_in?rd=$scheme://$http_host$request_uri;` truncates
#   the redirect at the first unencoded `&` in the original URL, because
#   oauth2-proxy parses `rd` as a query-string value (Go `req.Form.Get("rd")`).
#   Using the header avoids both problems with no extra nginx modules.
error_page 401 = @error401;

# pass information via X-User and X-Email headers to backend,
# requires running with --set-xauthrequest flag
auth_request_set $user   $upstream_http_x_auth_request_user;
auth_request_set $email  $upstream_http_x_auth_request_email;
proxy_set_header X-User  $user;
proxy_set_header X-Email $email;

# if you enabled --pass-access-token, this will pass the token to the backend
auth_request_set $token  $upstream_http_x_auth_request_access_token;
proxy_set_header X-Access-Token $token;

# if you enabled --cookie-refresh, this is needed for it to work with auth_request
auth_request_set $auth_cookie $upstream_http_set_cookie;
add_header Set-Cookie $auth_cookie;

# When using the --set-authorization-header flag, some provider's cookies can exceed the 4kb
# limit and so the OAuth2 Proxy splits these into multiple parts.
# Nginx normally only copies the first `Set-Cookie` header from the auth_request to the response,
# so if your cookies are larger than 4kb, you will need to extract additional cookies manually.
auth_request_set $auth_cookie_name_upstream_1 $upstream_cookie_auth_cookie_name_1;

# Extract the Cookie attributes from the first Set-Cookie header and append them
# to the second part ($upstream_cookie_* variables only contain the raw cookie content)
if ($auth_cookie ~* "(; .*)") {
    set $auth_cookie_name_0 $auth_cookie;
    set $auth_cookie_name_1 "auth_cookie_name_1=$auth_cookie_name_upstream_1$1";
}

# Send both Set-Cookie headers now if there was a second part
if ($auth_cookie_name_upstream_1) {
    add_header Set-Cookie $auth_cookie_name_0;
    add_header Set-Cookie $auth_cookie_name_1;
}
