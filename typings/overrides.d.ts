type OverrideVideo = {
  downlink: number;
  filename: string;
  durationSeconds: number;
  dateTime: string;
};

type OverridePhoto = {
  filenameRoot: string;
  directory: string;
  dateTimeOriginal: string;
};
