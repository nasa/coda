# Official framework image. Look for the different tagged releases at:
# https://hub.docker.com/r/library/node/tags/
image: node:14

# This folder is cached between builds
# http://docs.gitlab.com/ee/ci/yaml/README.html#cache
cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules/
    - client/.next/cache/

before_script:
  - npm install

stages:
  - test
  - build
  - pages
  - deploy_dev

test:
  script:
    - npm run lint
    - npm t
  stage: test

build:
  script:
    - npm run build
  stage: build
  artifacts:
    paths:
      - .next
      - client/.next

pages:
  script:
    - npm run export
    - mv client/out public
  stage: pages
  artifacts:
    paths:
      - public
  only:
    - live

deploy_dev:
  stage: deploy_dev
  environment:
    name: development
    url: https://coda-dev.fit.nasa.gov/coda_node
  before_script:
    ## See https://docs.gitlab.com/ee/ci/ssh_keys/index.html#ssh-keys-when-using-the-docker-executor
    ##
    ## Install ssh-agent if not already installed, it is required by Docker.
    ## (change apt-get to yum if you use an RPM-based image)
    ##
    - "command -v ssh-agent >/dev/null || ( apt-get update -y && apt-get install openssh-client -y )"

    ##
    ## Install rsync
    ##
    - "which rsync || ( apt-get update -y && apt-get install rsync -y )"

    ##
    ## Run ssh-agent (inside the build environment)
    ##
    - eval $(ssh-agent -s)

    ##
    ## Create the SSH directory and give it the right permissions
    ##
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh

    ##
    ## Add the FIT server host to known_hosts
    ##
    - echo "$SSH_KNOWN_HOSTS" >> ~/.ssh/known_hosts
    - chmod 644 ~/.ssh/known_hosts

    ##
    ## Add the SSH key stored in SSH_PRIVATE_KEY variable to the agent store
    ## We're using tr to fix line endings which makes ed25519 keys work
    ## without extra base64 encoding.
    ## https://gitlab.com/gitlab-examples/ssh-private-key/issues/1#note_48526556
    ##
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
  script:
    # sync the source code on the server at /home/bfeist/coda
    - rsync -acrvz --delete --exclude=node_modules --exclude=.env* --exclude=.git "$PWD" bfeist@jsfitdcoda.ndc.nasa.gov:/home/bfeist
    # rebuild the app then restart node on the VM
    - ssh bfeist@jsfitdcoda.ndc.nasa.gov "cd /home/bfeist/coda && npm i && systemctl --user restart coda-dev"
  only:
    - /^.*--deploy$/
